{
  "devDependencies": {
    "husky": "^8.0.3",
    "prettier": "^3.0.2"
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx,json,css,yml}": [
      "prettier --write"
    ]
  },
  "scripts": {
    "prepare": "husky install",
    "lint-staged": "lint-staged"
  },
  "dependencies": {
    "lint-staged": "^14.0.1"
  }
}
